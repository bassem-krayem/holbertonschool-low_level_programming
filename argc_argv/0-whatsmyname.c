#include "main.h"
#include <stdio.h>

/**
 * main - prints the program name
 * @argc: an unused variable.
 * @argv: the array of strings contains the arguments.
 *
 * Return: 0.
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
